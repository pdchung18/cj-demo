import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  searchBtn: {
    position: "absolute",
    // width: "textField",
    height: "40px",
    width: "100px",
    borderRadius: "5px",
  },
  textField: {
    width: "250px",
    height: "40px",
  },
  labelBtnStyle: {
    color: "white",
  },
});

export { useStyles };
